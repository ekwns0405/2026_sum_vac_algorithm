# 2026_sum_vac_algorithm
방학동안 알고리즘 공부

## 5주차: 세그먼트 트리 알고리즘
* 활동기간 : 2026년 8월 23일 일요일 20:00 ~ 23:00 (총 3시간)
* 모임 장소: Zoom회의 (온라인)
* 활동인원: 황재성, 김민규, 이다준

### 1. 활동 내용
#### 1) 세그먼트 트리 이론
* 배열의 연속한 구간에 대해 질의(query)와 갱신(update)을 효율적으로 수행할 수 있도록 설계된 이진 트리 기반 자료구조.
* 구간 합, 구간 최대/최소, 구간 곱과 같은 구간 연산들을 O(logN) 시간 만에 처리할 수 있다.

#### 2) 세그먼트 트리 설명
* 예를 들어 길이가 N인 배열 A가 주어진다.
* 그 다음 아래 두 명령들이 합쳐서 M개 주어진다.
  - 갱신 : 배열 Ai의 값을 x로 갱신한다.
  - 질의 : i부터 j까지의 원소들에 대한 연산의 값(Ai⊕...⊕Aj, ⊕는 특정 연산)을 출력한다.
* 만약 다음과 같은 연산들을 기본 배열로 관리하여 풀면 각 갱신은 O(1)만에 처리할 수 있지만, 질의는 (j-i)번이므로 O(N)이 걸린다.
* 질의를 빠르게 하는 방법으로 일전에 공부했던 누적합을 생각할 수 있는데, 이 경우 질의는 O(1)만에 처리할 수 있지만 갱신은 O(N)만큼 걸리기 때문에 시간복잡도는 비슷하다.
* 따라서 갱신과 질의의 시간 복잡도가 O(logN)인 세그먼트 트리를 이용하는 것이다.
* 세그먼트 트리의 아이디어는 배열을 구간들로 나눈 완전 이진 트리 구조로 만들어서 관리하는 것이다.
* 예를 들어 A=[3,4,2,1,5,6,7,8], a+b=a⊕b 이라고 할 때 다음과 같은 트리구조가 저장된다.
  - 트리  
  -<img width="577" height="89" alt="image" src="https://github.com/user-attachments/assets/4bf665a8-9ac8-4bf0-8d8f-7f7539ca29ce" />  
  - 갱신 : 만약 7을 -3으로 갱신하면 노란색 칸만 연산하면 된다.  
  -<img width="577" height="89" alt="image" src="https://github.com/user-attachments/assets/3edf7c0d-4d82-4093-96ec-f2caa1e73318" />  
  - 질의 : 만약 A2-A7까지 구하면 파란 칸만 연산하면 된다.  
  -<img width="577" height="89" alt="image" src="https://github.com/user-attachments/assets/8e281084-e1fe-46e2-bb15-ec8975b687fd" />  



#### 3) 세그먼트 트리 관련 예제
#####  구간의 최대값2
* 정올링크 : https://jungol.co.kr/problem/3238
* 문제 : 주어진 수열에 대하여 다음 3 가지 명령을 수행하는 프로그램을 작성하시오.  
수열의 데이터는 배열의 1번 인덱스부터 N번 인덱스에 걸쳐 저장된다고 가정한다.  ​
  - 1 k val : 수열의 k번째에 val을 입력한다. 이미 값이 입력되어 있다면 val로 수정한다.  
  - 2 s e   : 수열의 [s, e]구간의 최대값을 구하여 행으로 구분하여 출력한다. 출력할 것이 없다면 아무것도 출력하지 않는다.
  - 3 k     : 수열의 k번째 값을 지운다. 지울 것이 없다면 아무일도 하지 않는다.  

* 입력 : 첫 행에 수열의 길이 N과 명령의 수 M이 입력된다. (1 ≤​ N ≤​ 100,000, 1 ≤​ M ≤​ 300,000)  
수열의 각 원소가 갖는 수의 범위는 -10억 ~ 10억 이다. 다음 M개의 행에 명령들이 주어진다.

* 출력 : 명령 2에 대한 출력결과를 행으로 구분하여 출력한다. 명령 2에 대하여 출력할 값이 없는 경우 출력하지 않는다.
  
* 풀이 : 세그먼트 트리를 구현만 하면 되는 문제라 두가지 함수 update와 query를 만드는데 집중했다.  
  - 먼저 update는 위에 이론에서 말했던 갱신을 뜻하는데 입력값은 (int node, int start, int end, int index, long long val)인데 node는 말 그대로 트리의 노드를 말하고 start와 end는 범위, index와 val은 몇 번째 숫자를 어떤 값으로 업데이트 할지 정하는 1번 연산 때 사용하기 위해 받았다.  
    또한 비어있는 값을 의미하기 위해 처음에 배열을 min 값으로 모두 초기화했다. MIN값은 원소에 들어갈 수 있는 최솟값인 10^-9보다 작은 10^-11로 설정했다.  
    기저 조건으로 start==end 이면 그 값이 바뀔 값이니 tree[node] = val을 하고 끝내고 그 조건에 도달하기 까지 int mid = start + (end - start) / 2로 중간을 설정하고 update(node * 2, start, mid, index, val), update(node * 2 + 1, mid + 1, end, index, val) 두 방향으로 update를 반복하며  
    tree[node] = max(tree[node * 2], tree[node * 2 + 1]); 범위에서 최댓값을 update해준다.  
  - 다음으로 query에서 int node, int start, int end, int left, int right이 값들을 입력받는데 앞에 세 값들은 위와 같고 뒤에 left와 right는 범위이며 점점 줄여나가서 구간 내 최댓값을 구한다.
    기저 조건은 left <= start && right >= end이며 이때 tree[node]를 리턴한다. 하지만 만약 값이 MIN과 같다면 출력하지 않는다.
    return max(query(node * 2, start, mid, left, right), query(node * 2 + 1, mid + 1, end, left, right))이 값을 리턴한다.
    
### 2. 활동 인증 사진
<img width="1917" height="947" alt="image" src="https://github.com/user-attachments/assets/04b5c9d9-7e23-43b5-9312-ea182c4fbc8c" />
